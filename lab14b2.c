#include<stdio.h>
void main()
{
    int row,col;

    printf("enter row and column:");
    scanf("%d",&row);
    scanf("%d",&col);

    int a[row][col];

    for(int i=0;i<row;i++)
    {
        for(int j=0;j<col;j++)
        {
            printf("enter elments a[%d][%d]:",i,j);
            scanf("%d",a[i][j]);
        }
    }

    
    for(int i=0;i<row;i++)
    {
        for(int j=0;j)
    }
}